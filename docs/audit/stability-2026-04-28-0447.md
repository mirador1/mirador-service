# Stability Check — 2026-04-28-0447

Generated by `bin/dev/stability-check.sh` (183s) — composes
existing per-aspect scripts. See the script header for delegation.

## 🟡 ATTENTION (11)
- mirador-service-java: uncommitted changes — `cd /Users/benoitbesson/dev/mirador/mirador-service-java && git status`
- Docker disk: 369 GB used — run `docker system prune` (per CLAUDE.md)
- mirador-service-java: lefthook config present but .git/hooks/pre-commit missing — `lefthook install`
- mirador-ui: lefthook config present but .git/hooks/pre-commit missing — `lefthook install`
- Local Sonar at :9000 is DOWN — start with `docker compose --profile admin up -d sonarqube`
- UI: 1 `any` types in src/
- svc: 1 System.out/err call(s) in src/main — replace with SLF4J Logger
- mirador-ui README: broken local links: docs/compodoc/,docs/typedoc/
- mirador-service-java: ADR flat index drifted from files — run '/Users/benoitbesson/dev/mirador/mirador-service-java/infra/common/bin/dev/regen-adr-index.sh --in-place' and commit
- mirador-service-java: GitHub mirror 55 commits behind origin/main — `git push github origin/main:main`
- mirador-ui: GitHub mirror 25 commits behind origin/main — `git push github origin/main:main`

## ℹ️  Info (20)
- mirador-service-java: 71 commits ahead of origin/dev (push pending?)
- mirador-ui: 67 commits ahead of origin/dev (push pending?)
- healthcheck-all returned non-zero (services likely not running — OK if not needed)
- mirador-service-java: no main pipeline found (API unreachable?)
- mirador-ui: latest main pipeline = success
- mirador-ui: 1/5 recent main pipelines failed (trend)
- UI: 0 critical, 0 high CVEs
- Trivy: binary not installed — skip (brew install trivy)
- UI bundle delta: stats.json not found (run `npm run build -- --stats-json` first)
- Lighthouse: UI not on :4200 — skip (start with `npm start` in mirador-ui)
- Local Sonar :9000 DOWN — freshness check skipped
- UI: 2 console.* call(s) (≤3 acceptable; telemetry sink + main bootstrap excluded)
- svc: slice order — main 14f/841L, test 4f/706L
- svc: slice product — main 5f/370L, test 2f/340L
- svc: slice ml — main 7f/683L, test 5f/582L
- Terraform: all 4 modules validate + formatted
- All upstream images pinned (no :latest)
- Manual CI jobs not triggered (use --trigger-manual to opt in)
- Manual jobs never run on this pipeline: smoke-test,semgrep,mutation-test,build-native,compat-sb3-java17,compat-sb3-java21,compat-sb4-java17,compat-sb4-java21
- Trend: 🔴 0→0 (Δ0), 🟡 9→11 (Δ2) (attention growing)
