# realm-demo.json — test classpath copy

This file is loaded from the classpath by `KeycloakAuthITest` via `.withRealmImportFile("realm-demo.json")`.

It is an exact copy of `infra/keycloak/realm-demo.json`.
For full documentation of the realm structure (client, roles, users), see [`infra/keycloak/realm-demo.md`](../../infra/keycloak/realm-demo.md).

> **Keep in sync** — if you modify `infra/keycloak/realm-demo.json`, update this copy too.
